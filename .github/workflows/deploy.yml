name: Roq Site Deploy

on:
  push:
    branches: [ main ]
  workflow_run:
    workflows: ["Daily Digest"]
    types: [completed]
  workflow_dispatch:

jobs:
  build:
    if: ${{ github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Generate Roq Site
        uses: quarkiverse/quarkus-roq@v1.1
        with:
          java-version: '25'
          github-token: ${{ secrets.GITHUB_TOKEN }}   # Used to automatically get the GitHub Pages url
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    permissions:
      pages: write      # to deploy to Pages
      id-token: write   # to verify the deployment originates from an appropriate source
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
      - name: Notify IndexNow
        if: success()
        run: |
          curl -s -X POST "https://api.indexnow.org/indexnow" \
            -H "Content-Type: application/json; charset=utf-8" \
            -d '{"host":"devoured.fyi","key":"0D7BA173-F348-4235-8F27-DA39390BC11E","keyLocation":"https://devoured.fyi/0D7BA173-F348-4235-8F27-DA39390BC11E.txt","urlList":["https://devoured.fyi/","https://devoured.fyi/sitemap.xml"]}'
